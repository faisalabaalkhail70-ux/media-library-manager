# Single source of truth for the application version.
# Bump this string and create a matching GitHub Release tag (e.g. v1.1.0)
# for the updater to detect and offer the new version to users.
VERSION = "1.1.0"
